# RSI Heatmap Top-25

FastAPI backend (Binance klines + RSI) + frontend (HTML/JS) with mini charts and full-screen chart on click.

## Run locally

```bash
pip install -r requirements.txt
uvicorn backend.main:app --reload
